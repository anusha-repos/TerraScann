# Vite dev-server proxy target. This is not a browser secret.
SATQUERY_BACKEND_URL=http://127.0.0.1:8000

# Optional: only use this if the frontend must call a trusted same-origin /api endpoint directly.
# VITE_ANALYSIS_API_URL=/api/predict
